<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@color/super_toast_color_warning"
    android:gravity="center">

    //auto_tintColor：自动根据背景颜色设置图标白色还是黑色

    <ImageView
        android:id="@+id/img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:src="@drawable/ic_toast_super_error"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:tag="auto_tintColor" />
    //auto_textColor：自动根据背景颜色设置文字白色还是黑色

    <TextView
        android:id="@+id/msg"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:text="aaaaa"
        android:layout_marginRight="13dp"
        android:layout_toRightOf="@+id/img"
        android:background="@android:color/transparent"
        android:padding="10dp"
        android:tag="auto_textColor"
        android:textSize="14sp" />

</LinearLayout>